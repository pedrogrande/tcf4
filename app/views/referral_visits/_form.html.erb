<%= simple_form_for(@referral_visit) do |f| %>
  <%= f.error_notification %>
  <div class="form-group">
    <%= f.association :referrer, input_html: { class: 'form-control'} %>
  </div>
  <%= f.button :submit %>
  
<% end %>
